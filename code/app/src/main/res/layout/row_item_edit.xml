<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="20dp"
    android:minHeight="30dp"
    android:maxHeight="20dp"
    android:background="#eaeaea"
    android:orientation="horizontal" >

    <TextView
        android:id="@+id/item_title"
        android:layout_width="60dp"
        android:layout_height="match_parent"
	    android:background="@mipmap/icon_kuai_name"
	    android:gravity="center"
	    android:maxLines="1"
	    android:singleLine="true"
	    android:enabled="false"
        android:text="表头测试"
	    android:textColor="@color/white_ffffff" />

    <com.yzb.card.king.ui.appwidget.vhtable.CHTableScrollView
        android:id="@+id/item_scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"
        android:layout_weight="1" >

        <LinearLayout
        	android:id="@+id/item_scroll_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal" >
        </LinearLayout>
    </com.yzb.card.king.ui.appwidget.vhtable.CHTableScrollView>

	<TextView
		android:id="@+id/tvExchange"
		android:layout_width="60dp"
		android:layout_height="match_parent"
		android:background="@mipmap/icon_kuai_duihuan"
		android:gravity="center"
		android:maxLines="1"
		android:text="@string/intergral_exchange"
		android:textColor="#afafaf" />

</LinearLayout>